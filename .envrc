# Prefer system Node over Bun's shim for this repo.
if [ -x /usr/bin/node ] || [ -x /usr/bin/nodejs ]; then
  export PATH="/usr/bin:$PATH"
elif [ -x /usr/local/bin/node ] || [ -x /usr/local/bin/nodejs ]; then
  export PATH="/usr/local/bin:$PATH"
fi
