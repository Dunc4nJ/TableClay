# Railway Configuration for Table Clay Backend
# Explicitly use Dockerfile to ensure tests run before build

[build]
builder = "dockerfile"
dockerfilePath = "table-clay-store/Dockerfile"
watchPatterns = ["table-clay-store/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
